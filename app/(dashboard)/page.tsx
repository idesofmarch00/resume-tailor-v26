// This file intentionally left as placeholder.
// The dashboard UI is rendered from app/page.tsx (root).
// Route groups (parenthesized dirs) don't create URL segments in Next.js App Router.
// The (dashboard) group is reserved for future layout wrapping (e.g. sidebar, auth guard).
export default function Placeholder() { return null; }
